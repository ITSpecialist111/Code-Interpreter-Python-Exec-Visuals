From this data  <Document input Here>

Role: Performance Analyst
Goal: Create a clean "Lollipop Chart" to rank top performers.

Data Extraction Protocol:

Primary Method: Scan the PDF for a list of Regions, Products, or Channels and their Sales values. Look for a "Top 5" list.

Fallback Method: If extraction fails, use this Hardcoded Demo Data:

Items: ['Cloud Services', 'On-Premise', 'Consulting', 'Support', 'Training']

Values: [85, 60, 45, 30, 15] (Sorted High to Low).

Step 1: The Canvas (Strict Layout)

Initialize fig = plt.figure(figsize=(10, 6)).

Create main axis ax_main = fig.add_axes([0,0,1,1]) and turn it off (axis('off')).

Draw the white card background with a thin grey border.

Step 2: The Chart Layer

Create chart axis: ax_chart = fig.add_axes([0.25, 0.25, 0.7, 0.6]) (Shifted right to leave room for labels).

Nuclear Clean-Up: ax_chart.axis('off'). No borders, no ticks, no grid.

Step 3: Plotting (The "Lollipop" Logic)

The Stems: Draw horizontal lines for each item.

ax_chart.hlines(y=range(len(items)), xmin=0, xmax=values, color='#E0E0E0', linewidth=3)

The Heads: Draw circles at the end of each line.

ax_chart.scatter(values, range(len(items)), color='#0078D4', s=300, zorder=3) (Size 300 makes them big and bold).

Step 4: The Labels (Precision Placement)

Category Names (Left): Place the item names (e.g., "Cloud Services") to the left of the start of the line.

Use ax_chart.text(-5, i, item_name, ha='right', va='center', fontsize=12, color='grey').

Values (Inside the Head): Place the number (e.g., "85") inside the blue circle.

Use ax_chart.text(value, i, str(value), ha='center', va='center', fontsize=10, color='white', weight='bold').

Step 5: Text Elements

Title (Top Left): ax_main.text(0.05, 0.90, "Top Revenue Streams (YTD)", Size 16, Bold).

Narrative (Bottom): ax_main.text(0.05, 0.10, "Insight: Cloud Services now creates 2x the revenue of any other stream.", Size 12).

Final Output:
Display the image.
